{
  "homepage": "https://jmontoyajr.github.io/myFlix-client/",
  "name": "myflix-client",
  "version": "1.0.0",
  "description": "myFlix-Client application",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:watch": "npm run clean && parcel src/index.html",
    "clean": "rm -rf dist/*",
    "prod:build": "npm run clean && parcel build src/index.html --no-source-maps",
    "deploy": "npm run prod:build && cp CNAME dist && surge dist"
  },
  "author": "Jorge Montoya Jr",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "babel-preset-react": "^6.24.1",
    "prop-types": "^15.7.2",
    "react": "^17.0.2",
    "react-bootstrap": "^1.6.1",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.3.0"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0-rc.0",
    "parcel": "^2.0.0-rc.0",
    "surge": "^0.23.0"
  }
}